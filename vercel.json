{
  "buildCommand": "cd frontend && rm -rf .next node_modules/.cache && rm -f src/i18n/locales/en.json && npm install && npm run build",
  "outputDirectory": "frontend/.next",
  "devCommand": "cd frontend && npm run dev",
  "installCommand": "cd frontend && npm install",
  "framework": "nextjs",
  "regions": ["hkg1"],
  "env": {
    "NEXT_PUBLIC_LOCALE": "zh",
    "NEXT_PUBLIC_FORCE_REBUILD": "true"
  },
  "build": {
    "env": {
      "NEXT_PUBLIC_LOCALE": "zh"
    }
  }
}
